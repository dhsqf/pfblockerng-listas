name: Atualizar IPs Google

on:
  schedule:
    - cron: '0 5 * * *'  # 2h da manhã no Brasil (UTC−3)
  workflow_dispatch:

jobs:
  update-google:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Instalar dependência
        run: pip install requests

      - name: Executar script
        run: python scripts/atualiza_google.py

      - name: Commit e push (se houver alterações)
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          if git diff --quiet providers/google_ips.txt; then
            echo "Nenhuma alteração detectada."
          else
            git add providers/google_ips.txt
            git commit -m "Atualização Google: $(date +'%Y-%m-%d %H:%M:%S')"
            git push
          fi
